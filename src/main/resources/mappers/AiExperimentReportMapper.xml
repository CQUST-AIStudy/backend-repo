<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tap.backend.academic.dao.AiExperimentReportDao">

    <resultMap id="aiExperimentReportResultMap" type="com.tap.backend.academic.entity.AiExperimentReport">
        <id property="id" column="id"/>
        <result property="offeringId" column="offering_id"/>
        <result property="studentId" column="student_id"/>
        <result property="reportMd" column="report_md"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByOfferingAndStudent" resultMap="aiExperimentReportResultMap">
        SELECT id, offering_id, student_id, report_md, created_at, updated_at
        FROM ai_experiment_report
        WHERE offering_id = #{offeringId}
          AND student_id = #{studentId}
    </select>

    <insert id="upsert">
        INSERT INTO ai_experiment_report (offering_id, student_id, report_md)
        VALUES (#{offeringId}, #{studentId}, #{report})
        ON DUPLICATE KEY UPDATE
            report_md = VALUES(report_md),
            updated_at = CURRENT_TIMESTAMP(3)
    </insert>

</mapper>
