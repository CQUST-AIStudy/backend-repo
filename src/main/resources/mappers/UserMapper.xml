<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tap.backend.academic.dao.UserDao">
    <resultMap id="userResultMap" type="com.tap.backend.academic.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="usernum" column="usernum"/>
        <result property="classname" column="classname"/>
        <result property="phone" column="phone"/>
        <result property="department" column="department"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password_hash AS password, email, LOWER(role) AS role,
               usernum, classname
        FROM tap_user WHERE username = #{username}
    </select>

    <select id="findById" parameterType="int" resultMap="userResultMap">
        SELECT id, username, password_hash AS password, email, LOWER(role) AS role,
               usernum, classname
        FROM tap_user WHERE id = #{id}
    </select>

    <insert id="saveUser" parameterType="com.tap.backend.academic.entity.UserEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tap_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            username, password_hash, email, role, usernum, classname, display_name, enabled, created_at, updated_at,
            <if test="phone != null">phone,</if>
            <if test="department != null">department,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{username}, #{password}, #{email}, UPPER(#{role}), #{usernum}, #{classname},
            #{username}, TRUE, NOW(), NOW(),
            <if test="phone != null">#{phone},</if>
            <if test="department != null">#{department},</if>
        </trim>
    </insert>

    <update id="updateUser" parameterType="com.tap.backend.academic.entity.UserEntity">
        UPDATE tap_user
        <set>
            username = #{username},
            email = #{email},
            role = UPPER(#{role}),
            usernum = #{usernum},
            classname = #{classname},
            <if test="password != null">password_hash = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="department != null">department = #{department},</if>
            updated_at = NOW(),
        </set>
        WHERE id = #{id}
    </update>

    <select id="findByUsernameFromLegacyUser" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password AS password, email, LOWER(role) AS role,
               usernum, classname
        FROM user WHERE username = #{username} AND LOWER(role) = 'student'
    </select>

    <select id="findByUsernameFromLegacyUserAnyRole" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password AS password, email, LOWER(role) AS role,
               usernum, classname
        FROM user WHERE username = #{username}
    </select>

    <select id="findTapUserByUsernum" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password_hash AS password, email, LOWER(role) AS role,
               usernum, classname
        FROM tap_user WHERE usernum = #{usernum}
    </select>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>
